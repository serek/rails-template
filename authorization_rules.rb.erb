# declarative_authorization rules
authorization do
  # add custom rules here

  role :guest do
    has_permission_on :users, :to => [:new, :create]
  end
end

privileges do
  privilege :all, :includes => [:index, :show, :read, :new, :create, :edit, :update, :destroy]
end
